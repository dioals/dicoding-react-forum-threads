import React from 'react';
import ThreadCategory from '../components/styled/ThreadCategory';

export default {
  title: 'Thread/Category',
  component: ThreadCategory,
};

function Template() {
  return <ThreadCategory>Category</ThreadCategory>;
}

export const Category = Template.bind();
